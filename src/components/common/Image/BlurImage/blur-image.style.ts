import { styled } from "styled-components";

const BlurImageStyle = styled.div`
  width: 100%;
  height: 100%;
  filter: blur(5px);
  background-position: center;
  background-size: cover;
  background-repeat: no-repeat;
`;

export default BlurImageStyle;
